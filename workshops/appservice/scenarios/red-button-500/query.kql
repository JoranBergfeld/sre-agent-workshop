// Investigation query for Red Button 500.
// The VM investigation tool replaces {{VM_NAME}} with the target VM name.
AzureDiagnostics
| where TimeGenerated > ago(1h)
| where Resource contains "{{VM_NAME}}"
| take 50
