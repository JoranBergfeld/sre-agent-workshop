# Deploys the Cosmos RBAC Removal scenario web application to AKS.
name: Deploy Cosmos RBAC Removal Application

on:
  workflow_dispatch:
    inputs:
      workloadName:
        description: 'Workload name (must match the infra deployment)'
        type: string
        default: srelab

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SCENARIO_ROOT: scenarios/cosmos-rbac-removal
      WORKLOAD: ${{ inputs.workloadName || vars.WORKLOAD_NAME || 'srelab' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group "rg-${{ env.WORKLOAD }}" \
            --name "${{ env.WORKLOAD }}-aks" \
            --overwrite-existing

      - name: Fetch deployment values from Azure
        id: values
        run: |
          UAMI_CLIENT_ID=$(az identity show \
            --name "${{ env.WORKLOAD }}-id" \
            --resource-group "rg-${{ env.WORKLOAD }}" \
            --query clientId -o tsv)

          COSMOS_ENDPOINT=$(az cosmosdb list \
            --resource-group "rg-${{ env.WORKLOAD }}" \
            --query "[0].documentEndpoint" -o tsv)

          echo "uami_client_id=$UAMI_CLIENT_ID" >> "$GITHUB_OUTPUT"
          echo "cosmos_endpoint=$COSMOS_ENDPOINT" >> "$GITHUB_OUTPUT"

      - name: Apply Kubernetes manifests
        env:
          AZURE_CLIENT_ID: ${{ steps.values.outputs.uami_client_id }}
          COSMOSDB_ENDPOINT: ${{ steps.values.outputs.cosmos_endpoint }}
        run: |
          IMAGE_REPOSITORY="$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')/cosmos-rbac-removal"

          kubectl apply -f "${SCENARIO_ROOT}/k8s/namespace.yaml"

          sed "s|\${AZURE_CLIENT_ID}|${AZURE_CLIENT_ID}|g" "${SCENARIO_ROOT}/k8s/service-account.yaml" \
            | kubectl apply -f -

          sed -e "s|\${COSMOSDB_ENDPOINT}|${COSMOSDB_ENDPOINT}|g" \
              -e "s|IMAGE_REPOSITORY|${IMAGE_REPOSITORY}|g" \
              "${SCENARIO_ROOT}/k8s/deployment.yaml" \
            | kubectl apply -f -

          kubectl apply -f "${SCENARIO_ROOT}/k8s/service.yaml"

      - name: Wait for rollout
        run: kubectl rollout status deployment/web-app -n workshop --timeout=120s

      - name: Print app URL
        id: rollout
        run: |
          echo "Waiting for external IP (up to 60s)..."
          for i in $(seq 1 12); do
            IP=$(kubectl get svc web-app -n workshop -o jsonpath='{.status.loadBalancer.ingress[0].ip}' 2>/dev/null)
            if [ -n "$IP" ]; then
              break
            fi
            sleep 5
          done

          if [ -n "$IP" ]; then
            APP_URL="http://${IP}"
            echo "url=$APP_URL" >> "$GITHUB_OUTPUT"
            echo "App URL:  $APP_URL"
            echo "Health:   $APP_URL/health"
            echo "Items:    $APP_URL/items"
          else
            echo "External IP not yet assigned."
            echo "Run: kubectl get svc web-app -n workshop"
          fi
